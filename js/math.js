/*eslint-env browser*/

//STEP 1
//var number = window.prompt("Enter a number!");
//window.console.log("The absolute value of " + number + " is " + Math.abs(number) + ".");

//STEP 2
//var number = window.prompt("Enter a floating point value!");
//window.console.log("The rounded value of " + number + " is " + Math.round(number) + "!");

//STEP 3
//var number = window.prompt("Enter a floating point value!");
//window.console.log("The rounded down value of " + number + " is " + Math.floor(number) + "!");

//STEP 4
//var largest, smallest, numArray;
//    var num = window.prompt("Enter five numbers separated by commas. \rDO NOT ENTER SPACING!\r(Example: 1,2,3,4,5)\r");
//    numArray = num.split(",");
//    for (var i = 0, smallest = numArray[0], largest = numArray[0]; i < numArray.length; i++) {
//        if (numArray[i] < smallest) {
//            smallest = numArray[i];
//        }
//        if (numArray[i] > largest) {
//            largest = numArray[i];
//        }
//    }
//    window.console.log( smallest + " is the smallest number. While " + largest + " is the largest number!");

//STEP 5
var number = window.prompt("Enter a number!");
window.console.log("The square root of " + number + " is " + Math.sqrt(number) + "!");